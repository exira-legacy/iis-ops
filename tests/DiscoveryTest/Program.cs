namespace DiscoveryTest
{
    using System.Net;
    using Exira.EventStore;
    using Exira.IIS.Domain;

    class Program
    {
        static void Main(string[] args)
        {
            //var a = EventStore.connect(new EventStore.Configuration(IPAddress.Parse("127.0.0.1"), 1113, "admin", "changeit"));

            //var a = CommandHandler.parseCommand(5);
            //Console.WriteLine(a.IsFailure);
        }
    }
}
